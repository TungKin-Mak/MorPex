# AGENTS.md — {{PROJECT}} 开发规则（总纲）

> 规则分两层：本文件 = 总纲（必读，一页）；`docs/DEVELOPMENT.md` = 详细规范（改码前按需读）；进度在 `SESSION_LOG.md`。
> 强制：新会话开始只读 `AGENTS.md` + `SESSION_LOG.md`；其余文档按 §6 触发条件按需读，读完即弃。

## 1. 项目一句话
{{PROJECT}} — {{一句话}}（{{LANG}} / {{运行时}}）。

## 2. 仓库导航（一行）
{{顶层目录们的职责，一行一个，随真实结构更新}}

## 3. 常用命令（门禁）
```bash
{{BUILD_CMD}}      # 编译/类型检查（每次必跑）
{{ARCH_CMD}}       # 架构/合规校验（如有）
{{TEST_CMD}}       # 测试
{{DOC_SYNC_CMD}}   # 文档-代码一致性（如有，check:docs 类）
```
## 4. 铁律（速览 —— 违反即失败）
1. {{核心1：如"一切外部交互必经认证层"}}
2. {{核心2：如"模块间仅经消息总线"}}
3. {{核心3：如"唯一可 import 外部 SDK 的隔离点"}}
4. **文档同步（强制）**：改任何代码文件 → 必须同步更新相应文档（能力索引/职责/关系链/文件树，见 DEVELOPMENT §6）；无对应文档须在提交信息显式注明。
5. **禁止裸 `any` / 吞异常**（代码质量详则见 DEVELOPMENT）。

## 5. 规范流程（极简 —— 详见 docs/DEVELOPMENT.md §2）
```
① 定位 → 查 docs/CAPABILITY_INDEX.md（功能→锚点+别名，防 grep 误判/重复实现）；不盲目 grep 判"无"
② 理解 → 锚点函数关系链 + 业务流/消息 → 确定插在哪个函数前/后
③ 实现 → 只改锚点+hook 局部；过门禁（§3）
④ 收尾 → 更新文档（能力索引/职责/关系链/文件树/SESSION_LOG）→ 与代码同提交
```
## 6. 文档导航（按需读）
| 文档 | 何时读 |
|---|---|
| `SESSION_LOG.md` | 每次开工/收尾 |
| `docs/DEVELOPMENT.md` | 开发/改码前 |
| `docs/CAPABILITY_INDEX.md` | 新增/改功能前（功能→锚点） |
| `docs/HOOK_MAP.md` | 新功能插入时（接入点/前后） |
| {{其它核心文档}} | {{触发条件}} |

## 7. 会话约定
- 开始：只读 `AGENTS.md` + `SESSION_LOG.md`；结束：更新 `SESSION_LOG.md`「会话历史+待办」。
- 以代码为准（运行时 > 调用关系 > 测试 > 文档 > 设计）；文档与代码不符 → 修文档。