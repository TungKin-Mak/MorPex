# HOOK_MAP — {{PROJECT}} 接入点地图（新功能插哪 / 前·后）

> 开发流程第二步：`CAPABILITY_INDEX` 判"未实现"后，从这里选合法接入点，并按"前/后顺序"插入，保证数据流/消息不乱。
> 原则：优先扩展点，不改核心链（主干：{{入口 → 门禁 → 主执行 → 结尾 简述}}）。

## 1. 常用接入点速查
| 接入点 | 位置 | 前/后顺序 | 消息传递 | 举例 |
|---|---|---|---|---|
| {{新原语/能力}} | {{注册点}} | 注册后 → 可被消费 | {{总线/调用}} | {{例}} |
| {{新事件}} | {{emit 点 + 契约}} | 挂在对应生命周期后 | 订阅者经总线收 | {{例}} |
| {{人工决策}} | {{交互服务}} | 需输入时中断→等答复→继续 | {{事件}} | {{例}} |
| {{后置处理}} | {{评价/完成事件后}} | 主流程收尾后、进化前 | 订阅事件 | {{例}} |

## 2. 主流程各阶段"挂点"（按顺序）
```
{{入口}}
  ↓ {{门禁}} ──可插:{{…}}
  ↓ {{核心步骤1}} ──可插:{{…}}
  ↓ {{核心步骤n}} ──可插:{{…}}（后置/聚合）
  ↓ 收尾+持久化 ──可插:{{…}}
```

## 3. 别乱插
1. 不改核心主干（除非架构级需求）——新功能经事件/注册/新流程接入。
2. 消息字段按统一规格（{{事件/消息 spec}}）加块，不在旧 payload 续。
3. 插入后按 `DEVELOPMENT` §6 同步文档（索引/职责/关系链）。

## 4. 快速决策
| 你想要的 | 首选 hook |
|---|---|
| {{判要不要/怎么做}} | {{门禁/校验}} |
| {{让执行完多一步}} | 订阅 {{完成事件}} |
| {{要用户确认}} | {{交互服务}} |
| {{UI 多展示}} | {{投影/状态 API}} |