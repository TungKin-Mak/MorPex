# CAPABILITY_INDEX — {{PROJECT}} 能力索引（功能 → 锚点 + 别名 + 状态）

> 开发流程第一步「定位」：新增/改功能前先查索引，判断"是否已实现、在哪锚点"。**防**：重复实现 / 已实现未接入被误删 / grep 关键词不对误判"没有"。
> 维护：改哪条更新哪条（统一模板）；新增能力按域加行；发现"索引未覆盖但代码已有"→ 立即补条目（防漏判）。

## 0. 怎么用
1. 按「功能语义」在此查（含别名）→ 命中 = 已实现，看锚点决定**复用/扩展**；
2. 未命中 → 走 `HOOK_MAP` 找插入点；**不要在索引无命中时直接造轮子**。

## 1. {{能力域 A}}
| 能力 | 别名/同义词 | 锚点（文件·类/函数） | 状态 | 扩展怎么做 |
|---|---|---|---|---|
| {{功能1}} | {{别名1}} | {{文件}}·{{函数}} | ✅ 已接入（谁调用） | {{扩展建议}} |

## 2. {{能力域 B}}
| ... | ... | ... | ... | ... |

> 状态：✅ 已接入（有生产调用链）｜🟡 部分/待接入 ｜ 规划中。