# ── Node ──
node_modules/
dist/
*.tsbuildinfo
# 构建产物（xjmcu 插件默认写 build/<chip>）
build/
# 覆盖率临时产物（已配置写入 data/test-report/coverage/，此处防默认目录残留）
coverage/

# ── Runtime Data (自动生成) ──
data/
!data/README.md

# ── 构建产物 ──
packages/studio/ui/dist/

# ── 日志 ──
*.log
logs/

# ── 环境 ──
.env
.env.local

# ── 系统 ──
.DS_Store
Thumbs.db
nul

# ── 大文件 ──
*.zip
*.tar.gz

# ── Python ──
__pycache__/
*.pyc
.venv/

# ── 生成的分析报告 ──
.codebase-memory/
benchmark-reports/
repomix-output.xml

# ── 旧目录（迁移完成后删除） ──
# src/
# Pi/
# studio/

# ── 运行时 PID 文件 ──
.run-all.pids

# Obsidian editor layout (machine-specific, not project config)
.obsidian/workspace.json
.obsidian/workspace-mobile.json
