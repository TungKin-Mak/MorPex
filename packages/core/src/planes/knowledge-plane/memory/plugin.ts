import type { MorPexPlugin } from '../../../common/types.js';

export const MemoryPlugin: MorPexPlugin = {
  name: 'memory-plugin',
  version: '0.1.0',
  initialize: async () => {},
  start: async () => {},
  stop: async () => {},
};
